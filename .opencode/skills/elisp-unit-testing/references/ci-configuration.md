["push, pull_request]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    strategy:\n      matrix:\n        emacs_version: [26.3, 27.1, 28.1, 29.1]\n    steps:\n      - uses: actions/checkout@v3\n\n      - name: Set up Emacs\n        uses: purcell/setup-emacs@master\n        with:\n          version: ${{ matrix.emacs_version }}\n\n      - name: Set up Cask\n        uses: conao3/setup-cask@master\n\n      - name: Install dependencies\n        run: cask install\n\n      - name: Run tests\n        run: cask exec ert-runner\n```\n\n### With Coverage (Coveralls)\n\n```yaml\n      - name: Run tests with coverage\n        env:\n          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}\n          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_TOKEN }}\n          COVERALLS_PARALLEL: 1\n        run: |\n          UNDERCOVER_FORCE=true cask exec ert-runner\n\n  finish:\n    needs: test\n    runs-on: ubuntu-latest\n    if: always()\n    steps:\n      - name: Notify Coveralls\n        run: |\n          curl -L https://coveralls.io/webhook?repo_token=${{ secrets.COVERALLS_REPO_TOKEN }} \n            -d \"build_num=$GITHUB_RUN_NUMBER&status=done\"\n```\n\n### With Coverage (Codecov)\n\n```yaml\n      - name: Run tests\n        run: cask exec ert-runner\n\n      - name: Upload coverage\n        run: |\n          bash <(curl -s https://codecov.io/bash)\n```\n\n## Travis CI\n\n```yaml\nlanguage: emacs-lisp\n\nenv:\n  matrix:\n    - EMACS_VERSION=26.3\n    - EMACS_VERSION=27.1\n    - EMACS_VERSION=28.1\n\nbefore_install:\n  - curl -fsSkL https://raw.githubusercontent.com/rejeep/emacs-travis/master/emacs-travis.mk > .travis.mk\n  - make -f .travis.mk install\n\ninstall:\n  - make -f .travis.mk cask\n\nscript:\n  - cask exec ert-runner\n\nafter_success:\n  - bash <(curl -s https://codecov.io/bash)\n```\n\n## GitLab CI\n\n```yaml\ntest:\n  image: silex/emacs:27\n  script:\n    - curl -L https://cask.github.io/cask-go | python\n    - export PATH=\"$HOME/.cask/bin:$PATH", "cask install\n    - cask exec ert-runner\n\n  artifacts:\n    reports:\n      coverage_report:\n        coverage_format: cobertura\n        path: coverage/cobertura-coverage.xml\n```\n\n## Makefile Integration\n\n```makefile\nEMACS ?= emacs\nCASK ?= cask\n\n.PHONY: test test-batch test-coverage clean\n\ntest:\n    $(CASK) exec ert-runner\n\ntest-batch:\n    $(EMACS) -batch -L . -l ert -l test/*.el -f ert-run-tests-batch-and-exit\n\ntest-coverage:\n    UNDERCOVER_FORCE=true $(CASK) exec ert-runner\n\nclean:\n    rm -f **/*.elc\n    rm -rf coverage/\n```\n\n## CI Best Practices\n\n### Test Multiple Emacs Versions\n\nAlways test against minimum and latest stable Emacs. Include snapshot if cutting-edge features.\n\n### Cache Dependencies\n\n```yaml\n      - name: Cache Cask dependencies\n        uses: actions/cache@v3\n        with:\n          path: .cask/\n          key: ${{ runner.os }}-cask-${{ hashFiles('Cask') }}\n```\n\n### Parallel Execution\n\nUse matrix strategy for parallel tests across Emacs versions.\n\n### Notifications\n\n```yaml\n      - name: Notify on failure\n        if: failure()\n        uses: actions/github-script@v6\n        with:\n          script: |\n            github.rest.issues.createComment({\n              issue_number: context.issue.number,\n              owner: context.repo.owner,\n              repo: context.repo.repo,\n              body: 'Tests failed for Emacs ${{ matrix.emacs_version }}'\n            })\n```\n\n## Buttercup CI\n\n```yaml\n      - name: Run Buttercup tests\n        run: |\n          cask exec buttercup -L . --reporter spec\n```\n\n## Docker Integration\n\n```dockerfile\nFROM silex/emacs:28.1\n\nRUN curl -L https://cask.github.io/cask-go | python\nENV PATH=\"/root/.cask/bin:${PATH}\"\n\nCOPY . /app\nWORKDIR /app\n\nRUN cask install\nRUN cask exec ert-runner\n```\n\n## Local CI Testing\n\n```bash\n# Act like GitHub Actions\nact -j test\n\n# Test specific job\nact -j test --matrix \"emacs_version=28.1"]